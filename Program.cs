using System;

namespace HelloWorld // Namespace: A container for classes
{
    class Program // Corrected to PascalCase
    {
        static void Main(string[] args) // Main method
        {
            var x=true;
            var y=false;
            var z=false || true;
        Console.WriteLine(z);
        }
    }
}
